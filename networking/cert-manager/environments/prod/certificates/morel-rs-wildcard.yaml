apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: morel-rs-wildcard
  namespace: cert-manager
spec:
  secretName: morel-rs-wildcard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.morel.rs"
    - "morel.rs"
