apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: immich

resources:
  - namespace.yaml
  # Tenant guardrails (quota/limit/netpol) disabled while we stabilize.
  # - resource-quota.yaml
  # - limit-range.yaml
  # - network-policy.yaml
  - cnpg-cluster.yaml
  - redis.yaml
  - pvcs.yaml
  - deployment.yaml
  - ml-deployment.yaml
  - httproute.yaml
