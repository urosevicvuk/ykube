apiVersion: v1
kind: ResourceQuota
metadata:
  name: tenant-quota
  namespace: immich
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    requests.storage: 250Gi
    persistentvolumeclaims: "10"
    pods: "30"
