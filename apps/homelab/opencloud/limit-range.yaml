apiVersion: v1
kind: LimitRange
metadata:
  name: tenant-defaults
  namespace: opencloud
spec:
  limits:
    - type: Container
      default:
        cpu: "2"
        memory: 1Gi
      defaultRequest:
        cpu: 200m
        memory: 256Mi
