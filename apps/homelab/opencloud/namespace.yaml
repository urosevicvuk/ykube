apiVersion: v1
kind: Namespace
metadata:
  name: opencloud
  # PodSecurity labels removed while stabilizing. Re-add when ready:
  #   pod-security.kubernetes.io/enforce: restricted
  #   pod-security.kubernetes.io/warn: restricted
