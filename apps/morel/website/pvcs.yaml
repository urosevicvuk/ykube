apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: morel-data
  namespace: morel-website
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
