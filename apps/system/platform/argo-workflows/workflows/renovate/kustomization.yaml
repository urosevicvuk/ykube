apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# One self-contained workflow group: everything Renovate needs lives here.
# Add a sibling workflows/<name>/ dir + reference it from the parent
# kustomization to onboard another scheduled/triggered Workflow.
resources:
  - secrets.yaml
  - cronworkflow.yaml
