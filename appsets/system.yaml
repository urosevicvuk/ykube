# Single AppSet for all system plumbing. The foundation/networking/platform/
# observability subdirectories are kept for human navigation, but they don't
# drive AppSet sharding — Argo just retries failed first-sync attempts until
# the cluster converges (selfHeal + ServerSideApply). argocd self-takeover is
# in its own AppSet at wave +100 for upgrade safety; see system-argocd.yaml.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: system
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/urosevicvuk/ykube
        revision: main
        directories:
          - path: apps/system/*/*
          - path: apps/system/argocd/*
            exclude: true
  template:
    metadata:
      name: '{{path[2]}}-{{path.basename}}'
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: system
      source:
        repoURL: https://github.com/urosevicvuk/ykube
        targetRevision: main
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{path.basename}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - ServerSideApply=true
          - PruneLast=true
