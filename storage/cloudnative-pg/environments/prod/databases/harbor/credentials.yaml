apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: harbor-db-credentials
  namespace: harbor
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: harbor-db-credentials
  data:
    - secretKey: password
      remoteRef:
        key: secret/cloudnative-pg/harbor
        property: password
    - secretKey: username
      remoteRef:
        key: secret/cloudnative-pg/harbor
        property: username
